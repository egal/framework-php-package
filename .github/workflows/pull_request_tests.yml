name: Pull Request Tests

on:
  pull_request: { }

jobs:

  PHPUnit74:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Composer
        run: |
          docker-compose run --rm \
            --user "$(id -u):$(id -g)" \
            php \
            composer install \
              --dev \
              --no-interaction \
              --no-progress
      - name: Run a PHPUnit
        run: |
          export PHP_DOCKERFILE='php74.Dockerfile'
          bash bin/phpunit.sh

  PHPCS74:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: Install Composer
        run: |
          docker-compose run --rm \
            --user "$(id -u):$(id -g)" \
            php \
            composer install \
              --dev \
              --no-interaction \
              --no-progress
      - name: Run a PHPCS
        run: |
          DIFF="$(git diff \
            --name-only \
            --diff-filter=dr \
            ${{ github.event.pull_request.base.sha }} \
            ${{ github.sha }} | grep -E '(.php)$' || test $? = 1)"
          if [ "${DIFF}" == '' ]; then exit 0; fi
          export PHP_DOCKERFILE='php74.Dockerfile'
          bash bin/phpcs.sh --warning-severity=0 $DIFF

  PHPUnit80:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Composer
        run: |
          docker-compose run --rm \
            --user "$(id -u):$(id -g)" \
            php \
            composer install \
              --dev \
              --no-interaction \
              --no-progress
      - name: Run a PHPUnit
        run: |
          export PHP_DOCKERFILE='php80.Dockerfile'
          bash bin/phpunit.sh

  PHPCS80:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: Install Composer
        run: |
          docker-compose run --rm \
            --user "$(id -u):$(id -g)" \
            php \
            composer install \
              --dev \
              --no-interaction \
              --no-progress
      - name: Run a PHPCS
        run: |
          DIFF="$(git diff \
            --name-only \
            --diff-filter=dr \
            ${{ github.event.pull_request.base.sha }} \
            ${{ github.sha }} | grep -E '(.php)$' || test $? = 1)"
          if [ "${DIFF}" == '' ]; then exit 0; fi
          export PHP_DOCKERFILE='php80.Dockerfile'
          bash bin/phpcs.sh --warning-severity=0 $DIFF
